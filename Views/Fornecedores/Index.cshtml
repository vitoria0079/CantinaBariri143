@model IEnumerable<CantinaBariri143.Models.Fornecedores>

@{
    ViewData["Title"] = "Index";
}

<h1>Fornecedores</h1>

<div class="row mt-5 mb-3">
    <div class="col-3">
        <a asp-action="Create" class="btn btn-outline-danger w-100"><i class="bi bi-plus-circle"></i> Adicionar</a>
    </div>

    <div class="col-9">
        <!-- Campo de Busca -->
        <form asp-action="Search" method="get" class="mb-3">
            <div class="input-group">
                <input type="text" name="searchTerm" class="form-control" placeholder="Buscar..." />
                <button type="submit" class="btn btn-outline-danger"><i class="bi bi-search"></i> Buscar</button>
            </div>
        </form>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RazaoSocial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CNPJ)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RazaoSocial)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CNPJ)
                </td>
                <td>
                    <a asp-action="Edit" type="button" class="btn btn-dark text-light" asp-route-id="@item.FornecedoresId">Editar</a>
                    <a asp-action="Details" type="button" class="btn btn-dark text-light" asp-route-id="@item.FornecedoresId">Detalhes</a>
                    <a asp-action="Delete" type="button" class="btn btn-dark text-light" asp-route-id="@item.FornecedoresId">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
